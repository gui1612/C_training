#include <stdio.h>
#include <limits.h>

int main(){

	/*
	long long a = INT_MAX;
	long long b = 3;

	printf("%d + %d = %d\n", a, b, (a+b));
	printf("%d + %d = %d\n", a, b, (a*b));
	printf("%d + %d = %d\n", a, b, (a-b));
	printf("%d + %d = %d\n", a, b, (a/b));
	printf("%d + %d = %d\n", a, b, (a%b));
	*/

	double c = 1.5;
	double d = 2;

	printf("%.2lf\n", c);
	printf("%lf\n", c/d);
}
